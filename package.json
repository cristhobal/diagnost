{
  "name": "astro-doctor-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.1.0",
  "scripts": {
    "build": "tsc -b",
    "build:watch": "tsc -b -w",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "clean": "pnpm -r exec rimraf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
